Client dashboard: full-width cards + typography pass. All portal cards now span the full width on desktop (the two-column grid is gone). The monitoring and cloud cards use the extra room with the plan name at display size and price on the left, and the managed-by-McKee note in a divided column on the right. Card titles bumped to xl with tighter tracking, descriptions refined, section content sits under a divider for consistent rhythm, billing detail rows get baseline alignment and tabular numerals for amounts, and payment history rows have better contrast hierarchy. Gates re-run green.
