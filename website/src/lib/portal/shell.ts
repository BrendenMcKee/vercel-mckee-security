/**
 * Shared desktop shell for the staff console and the client portal.
 * Wide enough for tables (name, account chip, services) without wrapping
 * short values. Marketing pages keep their own widths.
 */
export const PORTAL_SHELL_CLASS = "mx-auto w-full max-w-[90rem] px-4";
